# 📚 BookSite - موقع مكتبة الكتب الإلكترونية

موقع احترافي لمشاركة ونشر الكتب الإلكترونية بسهولة وأمان كامل.

## ✨ المميزات

- ✅ عرض ديناميكي لقائمة الكتب مع واجهة احترافية
- ✅ بحث فوري عن الكتب حسب العنوان والمؤلف
- ✅ تصنيفات الكتب (روايات، علمية، تطوير شخصي، إلخ)
- ✅ معاينة الكتب مباشرة من المتصفح
- ✅ تحميل الكتب بصيغة PDF
- ✅ لوحة إدارة آمنة لإضافة كتب جديدة
- ✅ نظام رفع آمن بتحقق من الملفات
- ✅ استضافة على Netlify مع دالات serverless
- ✅ دعم اللغة العربية كاملاً (RTL)
- ✅ تصميم متجاوب يعمل على جميع الأجهزة

## 🚀 التطبيق الآن جاهز!

### للمستخدمين (المشاهدة):
1. افتح الموقع
2. تصفح الكتب المتاحة
3. استخدم البحث للعثور على كتاب معين
4. اختر فئة للتصفية
5. اضغط على أي كتاب لرؤية التفاصيل
6. اضغط "تنزيل الكتاب" أو "معاينة الكتاب"

### للمشرف (إضافة كتب):
1. اضغط على زر "M" في أسفل اليسار (أو استخدم `Ctrl+Shift+Q`)
2. أدخل مفتاح الإدارة: `bks-admin-key-2024-moaaz-secure`
3. ملأ بيانات الكتاب:
   - عنوان الكتاب
   - اسم المؤلف
   - التصنيف
   - ملخص قصير
   - ملف الغلاف (JPG/PNG)
   - ملف الكتاب (PDF)
4. اضغط "توليد كود الكتاب"
5. سيتم رفع الملفات تلقائياً

## 🔐 الأمان

- ✅ نظام مفاتيح إدارة آمن (API Key)
- ✅ التحقق من نوع الملفات
- ✅ حد أقصى لحجم الملفات (10MB للغلاف، 100MB للكتاب)
- ✅ معالجة آمنة للأخطاء
- ✅ رؤوس أمان CORS مطبقة
- ✅ منع الهجمات الشائعة (XSS, CSRF, etc)

## 📁 هيكل الملفات

```
BookSite/
├── index.html                 # الصفحة الرئيسية
├── package.json               # بيانات المشروع
├── netlify.toml              # إعدادات Netlify
├── js/
│   └── upload.js             # منطق رفع الكتب
├── netlify/
│   └── functions/
│       └── upload.js         # دالة الرفع على الخادم
├── books/                     # مجلد الكتب (PDFs)
├── covers/                    # مجلد الأغلفة (الصور)
└── README.md                 # هذا الملف
```

## 🛠️ التطوير المحلي

### المتطلبات:
- Node.js 14+ و npm
- Netlify CLI: `npm install -g netlify-cli`

### الخطوات:
```bash
# 1. تثبيت الحزم
npm install

# 2. تشغيل الخادم المحلي
npm run dev
# أو
netlify dev

# 3. افتح في المتصفح
# http://localhost:8888
```

## 📦 النشر على Netlify

### إعدادات Netlify:
البيانات التالية مضبوطة بالفعل في `netlify.toml`:
- **Site ID**: `6e50a864-8451-4085-9b16-e683e1dc9f8a`
- **Build Directory**: الجذر (`.`)
- **Functions Directory**: `netlify/functions`

### خطوات النشر:
```bash
# 1. تسجيل الدخول إلى Netlify
netlify login

# 2. ربط المشروع (إذا لم يكن مربوطاً)
netlify link

# 3. نشر الإصدار الإنتاجي
npm run deploy
# أو
netlify deploy --prod
```

## 📋 الكتب المتاحة حالياً

1. **مائة عام من العزلة** - غابرييل غارسيا ماركيز
2. **الإنسان يبحث عن المعنى** - فيكتور فرانكل
3. **الكون الأنيق** - براين جرين
4. **تاريخ موجز للزمان** - ستيفن هوكينج
5. **فن عدم الانتقام** - الرازي
6. **أرض زيكولا** - كاتب مجهول

## ⚙️ إضافة كتاب جديد

### الطريقة 1: عبر لوحة الإدارة (الأسهل)
1. افتح لوحة الإدارة
2. ملأ البيانات
3. اختر الملفات
4. اضغط الزر

### الطريقة 2: يدوياً (للمطورين)
1. أضف الملفات إلى `books/` و `covers/`
2. أضف كائن الكتاب إلى `booksData` في `index.html`
3. ادفع التغييرات إلى Git
4. انشر من جديد على Netlify

## 🐛 استكشاف الأخطاء

### لا تظهر الكتب:
- تحقق من console (F12)
- تأكد من أن `books/` و `covers/` يحتويان على الملفات الصحيحة

### الرفع لا يعمل:
- اضغط على "اختبار خدمة الرفع" في لوحة الإدارة
- تحقق من أن الدوال منشورة على Netlify
- تحقق من أن `NETLIFY_AUTH_TOKEN` و `NETLIFY_SITE_ID` مضبوطة

### مشاكل الأمان:
- تأكد من أنك تستخدم مفتاح الإدارة الصحيح
- لا تشارك مفتاح الإدارة مع أي شخص

## 📞 الدعم والمساعدة

للمزيد من المعلومات:
- 📖 [توثيق Netlify](https://docs.netlify.com)
- 🎓 [Tailwind CSS](https://tailwindcss.com)
- 📚 [JavaScript MDN](https://developer.mozilla.org)

## 📄 الترخيص

حقوق الملكية © 2025 BookSite - جميع الحقوق محفوظة

---

**ملاحظة مهمة**: هذا الموقع جاهز للاستخدام الفوري. فقط ارفع الملفات إلى Netlify واستمتع! 🎉
